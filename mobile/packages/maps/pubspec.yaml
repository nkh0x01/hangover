name: maps
description: MapProvider abstraction over Google Maps / Mapbox.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.9.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
