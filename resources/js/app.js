import './bootstrap';

// Alpine is bundled with Livewire 3 — do not start a second instance here.
// (Breeze ships an `import Alpine from 'alpinejs'; Alpine.start();` which
// causes "Detected multiple instances of Alpine running" and breaks
// wire:model initial-value rendering.)
