<?xml version="1.0" encoding="utf-8"?>
<!--
  Production: HTTPS only, no cleartext, certificate pinning placeholders.

  Dev / staging hosts allow cleartext (10.0.2.2 = Android emulator's
  host loopback; 192.168.* = LAN dev). These should be removed from
  the production build via build flavors before the play-store release.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Emulator + local LAN dev. Excluded from release flavor. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.43.0</domain>
    </domain-config>

    <!-- Pin the prod API cert (SHA-256) when staging is signed off.
         Until then, system trust is sufficient. -->
    <!--
    <domain-config>
        <domain includeSubdomains="true">api.hangover.app</domain>
        <pin-set expiration="2027-01-01">
            <pin digest="SHA-256">REPLACE_WITH_BASE64_SHA256_OF_LEAF_CERT</pin>
            <pin digest="SHA-256">REPLACE_WITH_BASE64_SHA256_OF_BACKUP_CERT</pin>
        </pin-set>
    </domain-config>
    -->
</network-security-config>
