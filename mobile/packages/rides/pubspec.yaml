name: rides
description: Ride domain models, repositories, and realtime event stream.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  core:
    path: ../core
  dio: ^5.7.0
  flutter:
    sdk: flutter
  maps:
    path: ../maps
  network:
    path: ../network
  realtime:
    path: ../realtime

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
